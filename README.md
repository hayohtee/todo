# todo
A command-line tool for managing a list of "to-do" item, keep track of items left in a project or activity and save the
list of items in a file using the JSON format

## Features
- Add a new todo
- Save todo to the current directory in JSON format
- Marking a todo as Complete
- Listing Uncompleted todos
